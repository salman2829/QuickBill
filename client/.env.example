# FRONTEND ONLY - copy to client/.env then run: npm run sync:config
# /api = same origin (recommended for Vercel full deploy)
# Or set a Render URL like: https://your-service.onrender.com/api

API_BASE_URL=/api
